<h1>All tasks</h1>

<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.title, task_path(task) %></li> <!--if click task, go to its details page (task being prefix for show route-->
  <% end %>
</ul>
